<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:av="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PolMedUMG.View"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="av" x:Class="PolMedUMG.View.dMainView"
             Height="auto"
             Width="auto">
    <UserControl.Resources>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background" Color="CornflowerBlue"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="DeepSkyBlue"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#FF3C7FB1"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#FFC4E5F6"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#FF2C628B"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
        <Style x:Key="RoundedButton" TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>

            <Setter Property="Content" Value="Szukaj"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" CornerRadius="3" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.MouseOver.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.MouseOver.Border}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Pressed.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Pressed.Border}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Disabled.Border}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20,0,20,20" ForceCursor="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="180"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>

        <TextBlock HorizontalAlignment="Center" Height="40" Margin="24,5,0,0" FontSize="26" FontWeight="Bold"  TextWrapping="Wrap" VerticalAlignment="Top" Width="200" Grid.Column="1" Language="en-150" Text="Strona główna"/>
        <!--Patient-->
        <TextBlock HorizontalAlignment="Center" FontSize="24" Grid.Row="1" Grid.Column="1" TextWrapping="Wrap" TextAlignment="Center" VerticalAlignment="Top" Width="350" Margin="0,40,20,0" Language="en-150" Text="Wyszukaj pacjenta" />
        <Label x:Name="LblPesel" Foreground="LightSlateGray" HorizontalAlignment="Center" Language="en-150" Height="28" IsHitTestVisible="False" Margin="-80,92,0,0" FontSize="16" Grid.Row="1"  Grid.Column="1" VerticalAlignment="Top" Width="240" Panel.ZIndex="1">
            <TextBlock Text="PESEL lub UID"/>
        </Label>
        <TextBox  x:Name="txtPatientSearch" TextChanged="txtPatientSearch_TextChanged" FontSize="22" HorizontalAlignment="Center" Height="32" Margin="-80,92,0,0"  Grid.Column="1" Grid.Row="1" Text="{Binding PatientPesel, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top" Width="240" />
        <Button Style="{DynamicResource RoundedButton}" Command="{Binding PatientLookup}"  x:Name="BtnPatientSearch"  BorderBrush="Transparent"  Grid.Column="1" HorizontalAlignment="Center" Height="32" Margin="370,92,50,0" Grid.Row="1" VerticalAlignment="Top" Width="90" />
        <!--/Patient-->

        <!--Doctor-->
        <TextBlock HorizontalAlignment="Center" FontSize="24"  Grid.Column="1" TextAlignment="Center" Grid.Row="2" VerticalAlignment="Top" Width="350" Margin="0,40,20,0" Text="Wyszukaj lekarza"/>
        <Label x:Name="LblDoctorId"  Foreground="LightSlateGray" HorizontalAlignment="Center" Height="28"  Grid.Column="1" IsHitTestVisible="False" Margin="-80,92,0,0" FontSize="16" Grid.Row="2"  VerticalAlignment="Top" Width="240" Language="en-150" Panel.ZIndex="1">
            <TextBlock Text="UID"/>
        </Label>
        <TextBox  x:Name="txtDoctorSearch" FontSize="22"  Text="{Binding DoctorUsername, UpdateSourceTrigger=PropertyChanged}"  Grid.Column="1" TextChanged="txtDoctorSearch_TextChanged" HorizontalAlignment="Center" Height="32" Margin="-80,92,0,20" Grid.Row="2" TextWrapping="Wrap" VerticalAlignment="Top" Width="240" />
        <Button x:Name="BtnDoctorSearch"  Command="{Binding DoctorLookup}"  BorderBrush="Transparent"  Grid.Column="1" HorizontalAlignment="Center" Height="32" Margin="370,92,50,0" Grid.Row="2" VerticalAlignment="Top" Width="90" Style="{DynamicResource RoundedButton}"/>
        <!--/Doctor-->


    </Grid>

</UserControl>
